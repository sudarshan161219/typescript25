//* Template Literal Types

// type Status = "success" | "error" | "pending";
// type StatusMessage = `status-${Status}`;

// type RGB = `rgb(${number}, ${number}, ${number})`;

// const color: RGB = "rgb(255, 45, 43)";
